[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 8080

[build.environment]
# Add any environment variables required at build time here or set them in the Netlify UI.
# Example:
# VITE_SUPABASE_URL = "https://..."
# VITE_SUPABASE_PUBLISHABLE_KEY = "public-anon-key"
